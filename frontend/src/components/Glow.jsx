const GlowEffect = () => (
    <div 
      className="absolute top-0 left-1/2 -translate-x-1/2 w-[1000px] h-[600px] pointer-events-none z-10 opacity-50 blur-[120px] animate-pulse"
      style={{
        background: 'radial-gradient(circle, #BFFF00 0%, transparent 70%)',
      }}
    />
  );